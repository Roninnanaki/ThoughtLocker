<%= form_for(current_user.relationships.find_by(followed_blog_id: @blog.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-lg btn-outline-secondary" %>
<% end %>
